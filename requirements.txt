# ============================================================
# AI英语口语陪练 (AI Spoken English Trainer)
# Core Dependencies
# ============================================================

# Backend API (FastAPI)
fastapi>=0.110.0
uvicorn>=0.27.0
pyjwt>=2.8.0
python-multipart>=0.0.6

# LLM & AI
openai>=1.12.0

# LangChain / LangGraph (AI Agent & Workflow Orchestration)
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.4.0
langsmith>=0.3.0

# Speech Services (Azure Pronunciation Assessment + TTS, Edge TTS)
azure-cognitiveservices-speech>=1.35.0
edge-tts>=7.0.0

# Environment & Config
python-dotenv>=1.0.0

# Database
# SQLite is built-in with Python, no extra install needed

# Chivox MCP — Phoneme-level Pronunciation Assessment
mcp>=1.20.0

# iFlytek ASR — WebSocket-based Speech Recognition
websockets>=12.0

# Audio Processing (optional — for server-side format conversion beyond WAV)
pydub>=0.25.0

# Utilities
requests>=2.31.0
httpx>=0.27.0
