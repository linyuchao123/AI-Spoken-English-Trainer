# 项目：AI英语口语陪练工具 | XEngineer第三批参赛项目

## 一、项目基础信息
1. 参赛选题：题目一 AI英语口语陪练
2. 开发周期：2026.06.05 00:00 ~ 2026.06.07 23:59（所有代码commit时间必须落在该时间段内）
3. 最终交付：开源GitHub仓库+可运行源码+完整README+带人声讲解的Demo演示视频
4. 技术架构：**Next.js 14 (前端) + FastAPI (后端)** ，JWT HTTP-only Cookie 认证 + GitHub OAuth，REST API 通信，SQLite (WAL 模式) 数据库

## 二、官方原题强制功能需求（必须全部实现，缺项扣分）
### 核心5项硬性功能
1. **场景选择**：固定3类练习场景：【职场面试 / 餐厅点餐 / 商务会议】，每个场景配置专属AI角色人设 + 视觉背景图；每个场景支持【初级/中级/高级】3个难度档位
2. **实时语音对话**：浏览器调用麦克风（Web Speech API），端到端低延迟语音交互；用户说英文，AI实时英文回复，TTS自动朗读（Edge TTS → Azure → OpenAI 三级降级），保证对话自然流畅
3. **发音评测打分**：用户每轮口语结束，自动进行英文发音评测，输出0~100分数 + 逐词发音定位（错误类型 + 音标提示）；当前方案为 Web Speech API 转写 + DeepSeek 文本对比评分，Azure 音素级评测预留接口
4. **语法&表达纠错**：在合适时机对用户语句做语法、用词、句式纠错（8类细分：grammar/vocabulary/word_order/preposition/article/tense/spelling/punctuation），标注原句错误+优化后的标准英文句子+中英双语解释+"更优表达"推荐，折叠卡片展示不打断实时对话
5. **课后量化总结报告**：单次对话练习结束后一键生成量化学习报告，包含：六维综合评分（语法/词汇/流利度/表达/自然度/情感）、逐句LLM深度分析、强弱项自动诊断、CEFR等级评估、针对性提升建议，实现口语能力可量化反馈

### 额外优化考量（题目隐性要求）
- 优化端到端语音延迟、对话自然度、纠错时机合理性，提升产品体验
- 四大创新维度：①对话交互自然度（三模型差异化风格注入+场景角色扮演+动态难度适应）②语音端到端流畅性（浏览器本地ASR+Edge TTS自动播放+三级降级）③纠错精准度与时机（对话优先纠错后置+8类细分+中英双语+更优表达）④可量化反馈（六维评估+逐句分析+评分趋势+CEFR等级）

## 三、竞赛评审三大打分规则（总分100，开发严格对标）
### 1. 作品完整度与创新性 40%
1. 完整落地上面全部5项基础功能，产品设计逻辑合理、页面交互流畅；
2. **创新加分自研设计（已实现）**
    - 每个场景增加【初级/中级/高级】难度档位，AI对话内容随难度自适应调整；
    - 课后量化报告用六维SVG圆环图/进度条展示评分，逐句分析+强弱项诊断；
    - 三模型差异化风格注入（GPT-4o 温暖鼓励 / GPT-4o-mini 简洁高效 / DeepSeek 耐心教学）；
    - TTS 三级降级链路（Edge TTS 免费零配置 → Azure → OpenAI），任一不可用时自动切换；
    - 页面间数据联动：ASR→Grammar、ASR→Pronunciation、Pronunciation→TTS 一键跳转；
    - 4个独立练习模块（发音评测/语法纠错/ASR转写/TTS合成），各含历史记录持久化

### 2. 开发过程与代码质量 40%（Git+PR是重点，违规直接无效作品）
> 规则红线：禁止最后一天一次性批量导入全部代码，必须分阶段PR提交，全周期持续commit
#### PR实际拆分（共18个PR，覆盖3天开发，每个PR单一任务规范）
1. PR #1~#7（6.5-6.6 框架+核心）：项目初始化 → 框架迁移 Streamlit→Next.js+FastAPI → LLM对话引擎 → 多模型风格注入 → 语法纠错 → 中英双语解释
2. PR #8~#15（6.6-6.7 语音+评测）：语法UI重构 → TTS三级引擎 → ASR语音识别 → 发音评测 → 课后报告 → 发音重构（Web Speech+DeepSeek）
3. PR #16~#18（6.7 前端+修复）：UI优化（场景背景/历史详情/多维评分/首页引导）→ 登录跳转修复 → Hydration错误修复
> PR格式强制要求：每个PR文档包含4部分：①标题（一句话改动内容）②功能描述 ③实现思路 ④自测验证方式

#### 代码规范：
- 项目前后端分离架构：前端 Next.js (TypeScript) / 后端 FastAPI (Python)；代码注释完善、可读性高
- Python 依赖统一写入 `requirements.txt`，前端依赖统一写入 `package.json`，README 中标明所有引用的开源库、第三方 API

### 3. 演示与表达 20%
- 录制完整Demo视频，全程人声讲解，按顺序演示：场景切换→实时语音对话→发音打分→语法纠错→生成量化报告→独立练习模块→技术栈总结 全流程；
- 视频上传百度网盘，链接写入README文档；
- README完整覆盖：项目背景/痛点/技术栈/数据库/API/本地运行/演示/创新点/开发过程/未来规划

