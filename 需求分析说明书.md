# AI英语口语陪练 — 需求分析说明书

> 版本：v2.0（已更新至实际实现） | 日期：2026-06-07 | 作者：XEngineer 第三批参赛项目

---

## 目录

1. [项目概述](#1-项目概述)
2. [需求分析](#2-需求分析)
3. [技术架构](#3-技术架构)
4. [技术栈详解](#4-技术栈详解)
5. [功能模块详细设计](#5-功能模块详细设计)
6. [数据库设计](#6-数据库设计)
7. [API 集成方案](#7-api-集成方案)
8. [UI/UX 设计规范](#8-uiux-设计规范)
9. [开发计划与 PR 拆分](#9-开发计划与-pr-拆分)
10. [测试策略](#10-测试策略)
11. [部署方案](#11-部署方案)
12. [风险与应对](#12-风险与应对)

---

## 1. 项目概述

### 1.1 项目背景

本项目为 XEngineer 新工科计划第三批竞赛选题——**题目一：AI 英语口语陪练**。旨在开发一款基于 AI 的英语口语练习工具，帮助用户在真实场景中进行英语口语对话训练，并获得即时的发音评分、语法纠正和课后量化学习报告，实现口语能力的可量化提升。

### 1.2 项目目标

| 目标 | 描述 |
|------|------|
| 场景对话 | 在职场面试、餐厅点餐、商务会议 3 个真实场景中进行英文对话练习 |
| 实时交互 | 浏览器麦克风收音，端到端低延迟语音交互，自然流畅 |
| 智能评测 | 每轮对话后自动发音评测（0-100 分）+ 语法纠错 |
| 量化反馈 | 课后一键生成可视化学习报告，包含趋势图、错误分析、提升建议 |

### 1.3 开发约束

| 约束项 | 说明 |
|------|------|
| 开发周期 | 2026-06-05 00:00 ~ 2026-06-07 23:59（72 小时） |
| 提交方式 | 公开 GitHub 仓库 + 18 次分阶段 PR 提交 |
| 编程语言 | Python 3.10+ (后端) / TypeScript (前端) |
| 前端框架 | Next.js 14 (App Router + TypeScript) + Tailwind CSS + Lucide Icons |
| 后端框架 | FastAPI + Uvicorn |
| 认证方式 | JWT HTTP-only Cookie + GitHub OAuth |
| 数据库 | SQLite |
| 交付物 | 可运行源码 + 完整 README + Demo 视频 |

---

## 2. 需求分析

### 2.1 功能性需求

#### F1. 场景选择与难度配置

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F1.1 | 3 个对话场景 | P0 必须 | 职场面试 / 餐厅点餐 / 商务会议 |
| F1.2 | 3 个难度档位 | P0 必须 | 初级 / 中级 / 高级 |
| F1.3 | 场景专属 AI 人设 | P0 必须 | 每个场景配置独立的 System Prompt |
| F1.4 | 难度自适应对话 | P0 必须 | AI 对话内容、语速、词汇随难度调整 |

**场景×难度矩阵（9 套配置）：**

| 场景 \ 难度 | 初级 Beginner | 中级 Intermediate | 高级 Advanced |
|-------------|---------------|-------------------|---------------|
| 职场面试 | 简单自我介绍、基础问答 | 行为面试、项目描述 | 技术深度讨论、薪资谈判 |
| 餐厅点餐 | 基础菜单词汇、简单句型 | 饮食需求、修改订单 | 投诉处理、定制需求 |
| 商务会议 | 简单议程讨论 | 进度汇报、意见表达 | 商务谈判、数据论证 |

#### F2. 实时语音对话

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F2.1 | 浏览器麦克风收音 | P0 必须 | 点击按钮开始/停止录音 |
| F2.2 | 流式语音识别 | P0 必须 | 实时将英文语音转为文本（ASR） |
| F2.3 | AI 英文回复 | P0 必须 | LLM 根据场景 Prompt 生成自然英文回复 |
| F2.4 | 语音合成播放 | P0 必须 | 将 AI 文本回复转为语音播放（TTS） |
| F2.5 | 低延迟交互 | P1 高优 | 端到端延迟控制在 3 秒以内 |
| F2.6 | 文本输入备用 | P2 中优 | 无麦克风时支持键盘文本输入 |

**核心对话流程：**

```
用户点击录音 → 浏览器 Web Speech API 本地识别 → 实时显示转写文本
→ 用户确认发送 → LLM 生成英文回复 → Edge TTS 自动朗读
→ 消息气泡下方折叠面板展示发音评分和语法纠错
```

#### F3. 发音评测打分

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F3.1 | 总分 0-100 | P0 必须 | 每轮用户语音后自动评测 |
| F3.2 | 分项得分 | P0 必须 | 准确度 / 流利度 / 完整度 |
| F3.3 | 错误词标注 | P1 高优 | 高亮发音错误词 + 正确音标 |
| F3.4 | 颜色分级展示 | P2 中优 | 绿色≥80 / 黄色 60-80 / 红色<60 |

#### F4. 语法与表达纠错

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F4.1 | 语法错误检测 | P0 必须 | 自动检测时态、冠词、介词等错误 |
| F4.2 | 优化句展示 | P0 必须 | 原句（错误标注）+ 优化后标准句式 |
| F4.3 | 错误分类标注 | P1 高优 | 标注错误类型（grammar/vocabulary/tense...） |
| F4.4 | 不打断对话 | P0 必须 | 纠错以折叠卡片展示，不打断实时对话 |

#### F5. 课后量化总结报告

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F5.1 | 整体平均分 | P0 必须 | 本次会话的整体发音平均分 |
| F5.2 | 发音趋势图 | P0 必须 | 每轮得分变化的折线图 |
| F5.3 | 错误分类饼图 | P0 必须 | 按错误类型分类的统计图表 |
| F5.4 | 高频错误 TOP5 | P0 必须 | 横向柱状图展示高频语法错误 |
| F5.5 | 薄弱知识点清单 | P1 高优 | 列出薄弱环节 + 例句 |
| F5.6 | AI 提升建议 | P1 高优 | 基于数据生成个性化口语提升建议 |
| F5.7 | 可视化图表 | P0 必须 | 使用自定义 SVG 组件（圆环图/进度条/词级卡片） |

### 2.2 非功能性需求

| ID | 需求 | 说明 |
|----|------|------|
| NF1 | 响应时间 | ASR 转写 < 2s，LLM 回复 < 3s，发音评测 < 2s |
| NF2 | 可用性 | 关键 API 失败时提供降级方案和用户提示 |
| NF3 | 兼容性 | 支持 Chrome / Edge 浏览器 |
| NF4 | 可扩展性 | 场景和难度 Prompt 配置化，易于新增 |
| NF5 | 代码质量 | 模块化分层，注释完善，符合 PEP 8 规范 |

---

## 3. 技术架构

### 3.1 架构总览图

```
┌──────────────────────────────────────────────────────────┐
│                    浏览器 (Browser)                       │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 麦克风   │  │ 对话界面  │  │ 评分展示  │  │ 报告页面  │  │
│  │ 录音     │  │ Chat UI  │  │ Score UI │  │ Report   │  │
│  └────┬─────┘  └──────────┘  └──────────┘  └──────────┘  │
│       │    Next.js 14 (React + TypeScript + Tailwind)    │
└───────┼──────────────────────────────────────────────────┘
        │  HTTP REST API (JSON) + JWT Cookie
        ▼
┌──────────────────────────────────────────────────────────┐
│                   FastAPI 后端 (Python)                    │
│                                                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ ASR 模块  │ │ LLM 模块 │ │ 发音评测  │ │ TTS 模块 │    │
│  │ Web Spch │ │GPT/Deep- │ │ Web+LLM  │ │ Edge/    │    │
│  │ API      │ │Seek API  │ │ 文本对比 │ │ Azure    │    │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘    │
│       │            │            │            │           │
│  ┌────┴────────────┴────────────┴────────────┴────┐      │
│  │              数据持久化层 (SQLite)                │      │
│  │  sessions │ messages │ scores │ corrections     │      │
│  └────────────────────────────────────────────────┘      │
└──────────────────────────────────────────────────────────┘
        │                          │
        ▼                          ▼
┌──────────────┐    ┌──────────────────────────┐
│ OpenAI API   │    │ Azure Speech Services    │
│ · GPT-4o     │    │ · Pronunciation Assess.  │
│ · GPT-4o-mini│    │ · Text-to-Speech         │
│ · TTS-1      │    └──────────────────────────┘
└──────────────┘
```

### 3.2 数据流图

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 用户点击  │───→│ 录音采集  │───→│ ASR转写  │───→│ LLM对话  │───→│ TTS播放  │
│ 录音按钮  │    │ (浏览器)  │    │(Web Spch)│    │ (GPT-4o) │    │ (OpenAI) │
└──────────┘    └──────────┘    └────┬─────┘    └────┬─────┘    └──────────┘
                    │                │               │
                    │   HTTP POST    │   HTTP POST   │
                    │   /api/sessions│  /api/sessions│
                    │   /{id}/audio  │  /{id}/messages│
                    ▼                ▼               ▼
              ┌──────────────────────────────────────────┐
              │            FastAPI 后端处理                │
              └──────────────────────────────────────────┘
                    │               │
         ┌──────────┘               └──────────┐
         ▼                                     ▼
   ┌──────────┐                        ┌──────────┐
   │ 发音评测  |                        │ 语法纠错  │
   │ (Azure/  |                        |          |
   | Web+LLM) │                        │ (LLM)    │
   └────┬─────┘                        └────┬─────┘
        │                                   │
        └───────────────┬───────────────────┘
                        ▼
                  ┌──────────┐
                  │ 数据存储  │
                  │ (SQLite) │
                  └────┬─────┘
                       │
                       ▼
                  ┌──────────┐
                  │ 报告生成  │
                  │ (后端聚合)│
                  └──────────┘
```

---

## 4. 技术栈详解

### 4.1 前端框架：Next.js 14

| 属性 | 说明 |
|------|------|
| 版本 | Next.js 14 (App Router) |
| 语言 | TypeScript |
| UI 库 | Tailwind CSS + Lucide Icons |
| 状态管理 | Zustand |
| HTTP 客户端 | Axios (withCredentials for JWT cookie) |
| 选型理由 | React 生态主流框架，App Router 支持服务端渲染与路由分组；Tailwind CSS 原子化样式高效开发，72h竞赛内快速迭代；Lucide Icons 提供1000+一致风格SVG图标 |
| 启动方式 | `npm run dev` → `http://localhost:3000` |

**Next.js 核心功能在本项目中的使用：**

| 功能 | 实现方式 | 用途 |
|------|---------|------|
| 页面路由 | App Router (文件系统路由) | 页面导航：login/register/practice/history/report |
| 布局 | Layout 组件 | 认证页布局 / 主应用布局（Navbar+Sidebar） |
| 侧边栏 | React 组件 + Zustand 状态 | 场景选择、难度选择、模型切换 |
| 状态管理 | Zustand store | 全局状态（用户/场景/难度/模型/会话） |
| Chat UI | React 组件 | 对话气泡展示 |
| API 通信 | Axios + JWT Cookie | 与 FastAPI 后端通信 |
| 样式 | Tailwind CSS | 响应式布局、深色侧边栏、渐变主题 |

### 4.2 语音识别：浏览器 Web Speech API（规划接入 OpenAI Whisper）

| 属性 | 说明 |
|------|------|
| 当前方案 | 浏览器原生 Web Speech API（SpeechRecognition） |
| 选型理由 | 语音识别在浏览器本地完成，无需上传音频到服务器，零延迟；Chrome/Edge 原生支持；兼容手动文本输入降级 |
| 规划方案 | OpenAI Whisper API（更高准确率，支持多口音，后续接入） |
| 输入 | 浏览器麦克风采集音频 |
| 输出 | 实时转写英文文本 |
| 降级方案 | 浏览器不支持语音识别时自动切换到手动文本输入 |

**当前使用流程：**

```
用户点击录音 → 浏览器 Web Speech API 采集并本地识别
→ 文字实时显示在输入框 → 用户确认后发送
```

**规划 Whisper 接入方案（后续版本）：**

```python
import openai

def transcribe_audio(audio_file_path: str) -> str:
    """使用 Whisper API 将音频转为文本"""
    with open(audio_file_path, "rb") as audio_file:
        transcript = openai.Audio.transcribe(
            model="whisper-1",
            file=audio_file,
            language="en",
            response_format="text"
        )
    return transcript
```


### 4.3 大语言模型：三模型差异化风格（GPT-4o / GPT-4o-mini / DeepSeek）

#### 4.3.1 三模型差异化风格

本项目不仅支持多模型切换，更为每个模型注入独立的系统 Prompt 风格指令：

| 模型 | 图标 | 风格特点 | 适用场景 |
|------|------|------|------|
| **GPT-4o** | 🧠 | 温暖自然、词汇丰富、鼓励式对话（模仿友善母语者） | 追求地道表达与语境丰富度 |
| **GPT-4o-mini** | ⚡ | 简洁直白、语言简单高效 | 快速问答、基础纠错反馈 |
| **DeepSeek** | 🔍 | 务实直接、教学语气、耐心指导（模仿老师） | 系统学习、需要详细解释 |

#### 4.3.2 OpenAI GPT-4o（主力模型）

| 属性 | 说明 |
|------|------|
| 模型 | `gpt-4o` |
| 选型理由 | 对话最自然流畅；英文理解/生成能力顶级；支持流式输出 |
| 费用 | 输入 $2.50/1M tokens，输出 $10.00/1M tokens |

**使用流程：**

```python
from openai import OpenAI

client = OpenAI(api_key="sk-xxx")

def generate_reply(messages: list, scene_prompt: str) -> str:
    """使用 GPT-4o 生成英文回复"""
    system_msg = {"role": "system", "content": scene_prompt}
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[system_msg] + messages,
        temperature=0.7,       # 保持一定创意性
        max_tokens=150,        # 限制回复长度
        stream=False           # PR2 阶段可开启 streaming
    )
    return response.choices[0].message.content
```

**System Prompt 设计要点：**

- 明确 AI 角色和场景
- 设定对话风格、语速、词汇难度
- 限制回复长度（2-4 句）
- 要求只使用英文
- 引导对话自然进行

#### 4.3.3 DeepSeek（备用模型）

| 属性 | 说明 |
|------|------|
| 模型 | `deepseek-chat` |
| 选型理由 | 国产大模型，成本极低（¥1/百万 tokens）；英文能力良好 |
| Base URL | `https://api.deepseek.com` |
| 费用 | 输入 ¥1/1M tokens，输出 ¥2/1M tokens |

**使用流程：**

```python
from openai import OpenAI

# DeepSeek 兼容 OpenAI SDK
client = OpenAI(
    api_key="sk-xxx",
    base_url="https://api.deepseek.com"
)

# 调用方式与 OpenAI 完全相同
response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[...]
)
```

**双模型切换方案：**

```python
# config/settings.py
DEFAULT_MODEL = "openai"  # 或 "deepseek"

# modules/llm.py
def get_llm_client():
    if st.session_state.current_model == "openai":
        return OpenAI(api_key=OPENAI_API_KEY)
    else:
        return OpenAI(
            api_key=DEEPSEEK_API_KEY,
            base_url=DEEPSEEK_BASE_URL
        )

def get_model_name():
    if st.session_state.current_model == "openai":
        return "gpt-4o"
    else:
        return "deepseek-chat"
```

### 4.4 发音评测：Web Speech + LLM 文本对比（规划接入 Azure）

| 属性 | 说明 |
|------|------|
| 当前方案 | Web Speech API 转写 + DeepSeek 文本对比评分 |
| 选型理由 | 浏览器本地语音转写零延迟；LLM 文本比对实现可用评分方案；无需额外 API 配置 |
| 规划方案 | Azure Pronunciation Assessment（音素级发音评测，学生账号审批中） |
| 输出 | 总分 0-100 + 逐词发音分析（准确度/错误类型/音标提示） |
| 降级方案 | Azure 不可用时自动回退到文本对比方案 |

**使用流程：**

```python
import azure.cognitiveservices.speech as speechsdk

def assess_pronunciation(audio_file: str, reference_text: str) -> dict:
    """使用 Azure 发音评测 API"""
    speech_config = speechsdk.SpeechConfig(
        subscription=AZURE_SPEECH_KEY,
        region=AZURE_SPEECH_REGION
    )

    # 配置发音评测
    pronunciation_config = speechsdk.PronunciationAssessmentConfig(
        reference_text=reference_text,    # 参考文本（用户的转写结果）
        grading_system="HundredMark",     # 百分制评分
        granularity="Phoneme",            # 音素级颗粒度
        enable_miscue=True                # 启用误读检测
    )

    audio_config = speechsdk.AudioConfig(filename=audio_file)
    recognizer = speechsdk.SpeechRecognizer(
        speech_config=speech_config,
        audio_config=audio_config
    )

    pronunciation_config.apply_to(recognizer)

    result = recognizer.recognize_once()

    # 解析结果
    pronunciation_result = speechsdk.PronunciationAssessmentResult(result)

    return {
        "overall_score": pronunciation_result.pronunciation_score,
        "accuracy_score": pronunciation_result.accuracy_score,
        "fluency_score": pronunciation_result.fluency_score,
        "completeness_score": pronunciation_result.completeness_score,
        "errors": _parse_word_errors(pronunciation_result)
    }
```

**返回数据结构：**

```json
{
    "overall_score": 78.5,
    "accuracy_score": 82.0,
    "fluency_score": 75.0,
    "completeness_score": 80.0,
    "errors": [
        {
            "word": "pronunciation",
            "correct_phoneme": "/prəˌnʌn.siˈeɪ.ʃən/",
            "error_type": "Mispronunciation"
        }
    ]
}
```

### 4.5 语音合成：Edge TTS（默认） → Azure → OpenAI 三级降级

#### Edge TTS（默认首选，免费零配置）

| 属性 | 说明 |
|------|------|
| 引擎 | Microsoft Edge TTS |
| 选型理由 | 完全免费、无需 API Key、合成速度 ~200ms；音质接近 Azure 神经语音；零配置开箱即用 |
| 格式 | mp3 |
| 音色 | en-US-AriaNeural / en-US-GuyNeural / en-US-JennyNeural / en-GB-SoniaNeural / en-GB-RyanNeural |

#### Azure TTS（高质量备选）

需配置 AZURE_SPEECH_KEY 和 AZURE_SPEECH_REGION。

#### OpenAI TTS（兜底备选）

| 属性 | 说明 |
|------|------|
| 模型 | `tts-1` |
| 音色 | `alloy`（自然中性英音） |
| 格式 | mp3 / opus / aac / flac |
| 费用 | $15.00 / 1M 字符 |

**使用流程：**

```python
from openai import OpenAI

def text_to_speech(text: str, output_path: str) -> str:
    """使用 OpenAI TTS 将文本转为语音"""
    client = OpenAI(api_key=OPENAI_API_KEY)
    response = client.audio.speech.create(
        model="tts-1",
        voice="alloy",          # 自然英音
        input=text
    )
    response.stream_to_file(output_path)
    return output_path
```

#### Azure TTS（备用）

与 Azure Speech 共用 SDK，无需额外配置。

### 4.6 数据可视化：自定义 SVG 组件

| 属性 | 说明 |
|------|------|
| 实现方式 | 纯前端自定义 SVG 组件（无需额外图表库依赖） |
| 选型理由 | 轻量零依赖；评分圆环/维度进度条/词级分析卡片完全定制化；无 Plotly 体积开销 |
| 图表类型 | 六维评分圆环图、维度进度条、颜色分级文字卡片 |

### 4.7 数据存储：SQLite

| 属性 | 说明 |
|------|------|
| 选型理由 | Python 内置支持，零配置；轻量级，适合本地运行；单文件数据库 |
| 数据库文件 | `data/sessions.db` |
| ORM | 直接使用 `sqlite3` 标准库，row_factory 返回 dict |

---

## 5. 功能模块详细设计

### 5.1 模块划分

```
modules/
├── llm.py           # 大模型对话模块（三模型切换+风格注入）
│   ├── get_client()           # 获取 LLM 客户端（GPT-4o/GPT-4o-mini/DeepSeek）
│   └── generate_reply()       # 生成 AI 英文回复（含动态开场白）
│
├── pronunciation.py # 发音评测模块（Web Speech + LLM 文本对比）
│   ├── assess()               # 发音评测（多轮打分+逐词分析）
│   └── format_errors()        # 格式化错误信息（含音标提示）
│
├── grammar.py       # 语法纠错模块（8类细分+双语解释）
│   ├── detect_errors()        # LLM 语法检测（8类错误类型）
│   └── format_correction()    # 格式化展示（含更优表达推荐）
│
├── tts.py           # 语音合成模块（三级降级）
│   ├── synthesize()           # Edge TTS → Azure → OpenAI 自动降级
│   └── get_tts_provider()     # 获取可用 TTS 服务商
│
├── evaluation.py    # 会话评估模块
│   └── evaluate_session()     # 六维综合评分+强弱项诊断
│
└── report.py        # 报告生成模块
    ├── aggregate_data()       # 聚合会话数据
    └── generate_suggestions() # AI 生成提升建议
```

### 5.2 语音交互流程（浏览器端实现）

音频采集和语音识别均在浏览器端通过 Web Speech API 完成，无需后端音频处理模块。降级方案为手动文本输入。

---

## 6. 数据库设计

### 6.1 ER 图（逻辑设计）

```
┌──────────────┐        ┌──────────────┐
│   sessions   │ 1───N │   messages   │
│              │       │              │
│ id (PK)      │       │ id (PK)      │
│ scene_key    │       │ session_id   │──→ FK → sessions.id
│ scene_name   │       │ role         │
│ difficulty   │       │ content      │
│ model        │       │ created_at   │
│ created_at   │       └──────┬───────┘
│ ended_at     │              │ 1───0..1
│ total_rounds │    ┌─────────┴─────────┐
│ avg_score    │    │                   │
│ status       │    ▼                   ▼                ▼
└──────────────┘  ┌──────────────────┐ ┌──────────────────────┐ ┌──────────────────────┐
                  │pronunciation     │ │ grammar_corrections  │ │ session_evaluations  │
                  │    _scores       │ │                      │ │                      │
                  │                  │ │ id (PK)              │ │ id (PK)              │
                  │ id (PK)          │ │ session_id (FK)      │ │ session_id (FK)      │
                  │ session_id (FK)  │ │ message_id (FK)      │ │ grammar_score        │
                  │ message_id (FK)  │ │ original_text        │ │ vocabulary_score     │
                  │ overall_score    │ │ corrected_text       │ │ fluency_score        │
                  │ accuracy_score   │ │ error_type           │ │ expression_score     │
                  │ fluency_score    │ │ explanation          │ │ naturalness_score    │
                  │ completeness     │ │ created_at           │ │ emotion_score        │
                  │ error_details    │ └──────────────────────┘ │ overall_level        │
                  │ created_at       │                          │ strengths/weaknesses │
                  └──────────────────┘                          │ suggestions          │
                                                               │ created_at           │
                                                               └──────────────────────┘
```

### 6.2 表结构详细设计

#### 表 1: sessions（会话表）

```sql
CREATE TABLE sessions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    scene_key       TEXT NOT NULL,          -- 场景 key: job_interview/restaurant/business_meeting
    scene_name      TEXT NOT NULL,          -- 场景中文名
    difficulty      TEXT NOT NULL,          -- 难度: beginner/intermediate/advanced
    model           TEXT NOT NULL,          -- 模型: openai/deepseek
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    ended_at        TIMESTAMP,             -- 结束时间（NULL=进行中）
    total_rounds    INTEGER DEFAULT 0,     -- 总对话轮数
    avg_pronunciation_score REAL DEFAULT 0.0, -- 平均发音分
    status          TEXT DEFAULT 'active'  -- active/ended
);
```

#### 表 2: messages（消息表）

```sql
CREATE TABLE messages (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id      INTEGER NOT NULL,      -- FK → sessions.id
    role            TEXT NOT NULL,         -- user / ai
    content         TEXT NOT NULL,         -- 消息内容
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE
);
```

#### 表 3: pronunciation_scores（发音评分表）

```sql
CREATE TABLE pronunciation_scores (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id      INTEGER NOT NULL,      -- FK → sessions.id
    message_id      INTEGER NOT NULL,      -- FK → messages.id
    overall_score   REAL NOT NULL,         -- 总分 0-100
    accuracy_score  REAL,                  -- 准确度
    fluency_score   REAL,                  -- 流利度
    completeness_score REAL,               -- 完整度
    error_details   TEXT,                  -- JSON: [{word, phoneme, error_type}]
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE,
    FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE
);
```

#### 表 4: grammar_corrections（语法纠错表）

```sql
CREATE TABLE grammar_corrections (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id      INTEGER NOT NULL,      -- FK → sessions.id
    message_id      INTEGER NOT NULL,      -- FK → messages.id
    original_text   TEXT NOT NULL,         -- 用户原句
    corrected_text  TEXT NOT NULL,         -- 纠正后句子
    error_type      TEXT,                  -- 错误类型
    explanation     TEXT,                  -- 错误解释
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE,
    FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE
);
```

#### 表 5: session_evaluations（会话评估表）

```sql
CREATE TABLE session_evaluations (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id      INTEGER NOT NULL,      -- FK → sessions.id
    grammar_score   REAL,                  -- 语法评分
    vocabulary_score REAL,                 -- 词汇评分
    fluency_score   REAL,                  -- 流利度评分
    expression_score REAL,                 -- 表达评分
    naturalness_score REAL,                -- 自然度评分
    emotion_score   REAL,                  -- 情感互动评分
    overall_level   TEXT,                  -- CEFR 等级评估
    strengths       TEXT,                  -- 强项分析
    weaknesses      TEXT,                  -- 弱项分析
    suggestions     TEXT,                  -- 提升建议
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE
);
```

---

## 7. API 集成方案

### 7.1 API 配置清单

| API | 环境变量 | 用途 | 申请地址 |
|-----|---------|------|---------|
| OpenAI API Key | `OPENAI_API_KEY` | GPT-4o/GPT-4o-mini 对话 + TTS 备选 | https://platform.openai.com |
| DeepSeek API Key | `DEEPSEEK_API_KEY` | 备选对话 + 发音文本对比 | https://platform.deepseek.com |
| Azure Speech Key | `AZURE_SPEECH_KEY` | 发音评测 + TTS 备选（可选） | https://portal.azure.com |
| Azure Region | `AZURE_SPEECH_REGION` | 服务区域 | eastasia（东亚） |
| JWT Secret | `JWT_SECRET_KEY` | 用户认证令牌签名 | 自定义随机字符串 |
| GitHub Client ID | `GITHUB_CLIENT_ID` | GitHub OAuth 登录（可选） | https://github.com/settings/developers |
| GitHub Client Secret | `GITHUB_CLIENT_SECRET` | GitHub OAuth 登录（可选） | https://github.com/settings/developers |
| Frontend URL | `FRONTEND_URL` | OAuth 回调跳转地址 | http://localhost:3000 |

### 7.2 环境变量管理

```ini
# .env 文件（不提交到 Git）
OPENAI_API_KEY=sk-your-openai-api-key
DEEPSEEK_API_KEY=sk-your-deepseek-api-key
AZURE_SPEECH_KEY=your-azure-speech-key
AZURE_SPEECH_REGION=eastasia
JWT_SECRET_KEY=your-jwt-secret-change-in-production
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
FRONTEND_URL=http://localhost:3000
```

### 7.3 错误处理与重试策略

| 场景 | 策略 |
|------|------|
| API 超时 | 设置 30s 超时，重试 3 次 |
| API 限额 | 提示用户 "API 配额已用尽，请稍后再试" |
| 网络异常 | 显示降级提示，保留文本输入备用通道 |
| 音频不可用 | 显示 "未检测到麦克风设备"，切换到文本输入模式 |
| 发音评测失败 | 显示 "评分暂时不可用"，不影响对话继续 |

---

## 8. UI/UX 设计规范

### 8.1 设计风格

| 属性 | 值 |
|------|-----|
| 设计风格 | 现代简约教育类应用 |
| 主色调 | 蓝色系（#667eea → #764ba2） |
| 背景 | 浅灰渐变（#f5f7fa → #c3cfe2） |
| 侧边栏 | 深色（#1a1a2e → #0f3460） |
| 字体 | Inter（Sans-serif） |
| 圆角 | 8px-18px |
| 阴影 | 柔和投影（透明度 6%-8%） |

### 8.2 页面布局

```
┌──────────┬─────────────────────────────────────────┐
│          │  ┌─────────────────────────────────┐    │
│ 侧边栏   │  │   English Conversation Practice  │    │
│ 300px    │  ├─────────────────────────────────┤    │
│          │  │                                  │    │
│ 🎤 场景  │  │  💬 AI: Welcome to the...        │    │
│ 选择     │  │                                  │    │
│          │  │  💬 You: I graduated from...     │    │
│ 📊 难度  │  │                                  │    │
│ 选择     │  │  📊 Pronunciation: 82分          │    │
│          │  │  📝 Grammar: 1 correction        │    │
│ 🤖 模型  │  │                                  │    │
│ 选择     │  │  💬 AI: That's great! Can...     │    │
│          │  │                                  │    │
│ ──────── │  ├─────────────────────────────────┤    │
│ 会话管理  │  │       🎤 [点击开始说话]         │    │
│ [新建]  │  │     Type your message...          │    │
│ [结束]  │  └─────────────────────────────────┘    │
│ ──────── │                                         │
│ 📈 报告  │                                         │
│ [生成]  │                                         │
└──────────┴─────────────────────────────────────────┘
```

### 8.3 交互规范

| 交互 | 规范 |
|------|------|
| 场景切换 | 弹出确认提示，清空对话历史 |
| 新建会话 | 自动结束上一会话，创建新会话 |
| 录音按钮 | 红色脉冲动画，录音中高亮 |
| 发音评分 | 即时展示，颜色分级 |
| 语法纠错 | 折叠卡片，点击展开 |
| 报告生成 | 独立页面渲染，支持滚动 |

### 8.4 颜色语义

| 颜色 | 含义 | 使用场景 |
|------|------|---------|
| 绿色 #00b894 | 优秀 | 评分 ≥ 80 |
| 黄色 #f39c12 | 良好 | 评分 60-80 |
| 红色 #d63031 | 需改进 | 评分 < 60 |
| 蓝色 #2196f3 | 信息 | 状态提示 |
| 橙色 #ff9800 | 警告 | 纠错卡片 |

---

## 9. 开发计划与 PR 拆分

### 9.1 PR 实际拆分总览（共 18 个 PR）

| 阶段 | PR 编号 | 日期 | 核心内容 |
|------|---------|------|---------|
| 🚀 启动 | #1 | 6.5 | 项目初始化、需求文档、基础框架搭建 |
| 🔄 迁移 | 框架迁移 | 6.6 | 从 Streamlit 全面迁移至 Next.js + FastAPI |
| 🧠 LLM | #6~#7 | 6.6 | LLM 对话引擎 + 多模型扩展与风格注入 |
| ✏️ 语法 | #8~#10 | 6.6 | 语法纠错模块 + 中英双语解释 + UI 重构 |
| 🔊 语音 | #11~#12 | 6.7 | TTS 三级引擎 + ASR 语音识别 |
| 📊 评测 | #13~#15 | 6.7 | 发音评测 + 课后报告 + 发音方案重构 |
| 🎨 前端 | #16~#17 | 6.7 | 首页引导 + 场景背景 + 历史详情 + 多维评分 UI |
| 🐛 修复 | #16, #18 | 6.7 | OAuth 编码修复 + 登录跳转修复 + Hydration 修复 |

### 9.2 核心阶段任务清单（全部已完成）

- [x] 项目目录结构搭建（前后端分离）
- [x] `requirements.txt` 依赖配置 + `package.json` 前端依赖
- [x] `config/settings.py` 全局配置加载
- [x] `config/prompts.py` 9 套场景×难度 Prompt
- [x] `utils/db.py` SQLite 数据库 CRUD 封装（6 表 + 索引 + WAL 模式）
- [x] `backend/main.py` FastAPI 主入口 + CORS
- [x] `backend/api/auth.py` JWT 注册/登录/登出/me 接口
- [x] `backend/api/scenes.py` 场景与难度配置接口
- [x] `backend/api/sessions.py` 会话 CRUD + 消息收发接口
- [x] `backend/core/auth.py` JWT 认证核心模块
- [x] `backend/models/schemas.py` Pydantic 请求/响应模型
- [x] `frontend/` Next.js 14 项目初始化（App Router + Tailwind + Lucide Icons）
- [x] 登录/注册页面
- [x] 主布局（Navbar + Sidebar）+ 场景/难度/模型选择
- [x] Practice 对话页面（Chat UI + 文本输入）
- [x] History 历史会话页面
- [x] Grammar/Pronunciation/ASR/TTS/Report/History 全部页面开发完成
- [x] Zustand 状态管理（auth-store + app-store）
- [x] `.env.example` 环境变量模板（7 项配置）+ `.gitignore`

### 9.3 语意交互阶段（6.6 — 已完成）

**后端核心模块开发：**

- [x] `modules/llm.py` — OpenAI/DeepSeek 双模型对话引擎
- [x] `modules/grammar.py` — LLM 语法纠错模块
- [x] `modules/asr.py`（浏览器端 Web Speech API，零延迟本地语音识别）
- [x] `modules/tts.py`（Edge TTS 三级降级） — TTS 文本转语音合成
- [x] 完善 `backend/api/sessions.py` send_message 接口（替换占位符）
- [x] 新增 TTS 音频生成接口 + 语法纠错接口
- [x] 新增发音评测接口 + 报告生成接口

**前端对接：**

- [x] Practice 页面对接真实 LLM 对话 API
- [x] 语法纠错卡片展示（折叠面板+双语解释）
- [x] 前端麦克风录音（Web Speech API）+ 手动输入降级
- [x] TTS 音频自动播放（Edge TTS 默认）

**目标：** 完成 "说话→转写→AI回复→语音播放→纠错" 完整闭环

### 9.4 评测报告阶段（6.7 — 已完成）

**发音评测：**

- [x] `modules/pronunciation.py` — Web Speech + LLM 文本对比发音评测
- [x] 发音评测接口 + 每轮对话后自动评测
- [x] 评分卡片 UI 展示（颜色分级+逐词分析）

**报告生成：**

- [x] `modules/report.py` + `modules/evaluation.py` — 六维评估
- [x] 报告接口（六维评分 + AI 强弱项诊断）
- [x] Report 页面数据对接（自定义 SVG 圆环/进度条）

**UI 优化 + 收尾：**

- [x] Grammar/Pronunciation/ASR/TTS 全部页面数据对接完成
- [x] 加载动画 + 状态提示优化
- [x] 桌面端完整适配（移动端适配后续规划）
- [x] 异常容错处理（API 降级 + 手动输入备用）

**最终交付：**

- [x] 端到端手动功能验证
- [x] README 完整版（880 行）
- [x] Demo 视频录制完成

---

## 10. 测试策略

### 10.1 测试范围

| 测试类型 | 内容 |
|---------|------|
| 单元测试 | 各模块独立功能验证 |
| 集成测试 | API 调用链路验证 |
| 端到端测试 | 完整用户操作流程 |
| 异常测试 | API 失败、网络异常、无效输入 |

### 10.2 测试用例

| ID | 场景 | 操作 | 预期结果 |
|----|------|------|---------|
| T1 | 场景切换 | 选择不同场景 | 场景更新，历史清空 |
| T2 | 新建会话 | 点击「新建会话」 | 数据库创建 session 记录 |
| T3 | 语音转写 | 录入英文语音 | 正确转写为英文文本 |
| T4 | AI 对话 | 发送英文文本 | AI 返回场景相关的英文回复 |
| T5 | 发音评测 | 每次用户语音后 | 显示 0-100 分 + 分项得分 |
| T6 | 语法纠错 | 输入含错英文 | 显示原句/纠正句/错误解释 |
| T7 | 报告生成 | 点击「生成报告」 | 展示图表 + 建议清单 |
| T8 | 模型切换 | 切换 OpenAI/DeepSeek | 对话模型切换生效 |
| T9 | 网络异常 | 断开网络后操作 | 显示友好错误提示 |
| T10 | 无麦克风 | 无麦克风设备 | 自动切换到文本输入模式 |

---

## 11. 部署方案

### 11.1 本地部署

```bash
# 克隆仓库
git clone https://github.com/linyuchao123/AI-Spoken-English-Trainer.git
cd AI-Spoken-English-Trainer

# === 后端部署 ===
# 创建虚拟环境
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # macOS/Linux

# 安装 Python 依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 填入 API 密钥

# 启动后端
uvicorn backend.main:app --reload --port 8000

# === 前端部署 ===
cd frontend

# 安装前端依赖
npm install

# 配置前端环境变量（如需自定义后端地址）
# NEXT_PUBLIC_API_URL=http://localhost:8000

# 启动前端开发服务器
npm run dev
```

启动后：
- 后端 API：`http://localhost:8000`
- 前端页面：`http://localhost:3000`

### 11.2 Vercel 部署（前端，可选）

1. 将代码推送到 GitHub 公开仓库
2. 访问 https://vercel.com 导入项目
3. 设置 Root Directory 为 `frontend`
4. 配置环境变量 `NEXT_PUBLIC_API_URL` 指向后端地址
5. 一键部署

---

## 12. 风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|---------|
| Azure 发音评测延迟高 | 中 | 用户体验差 | 异步调用 + 加载动画 |
| Whisper 英文口音不准 | 低 | 对话质量下降 | 指定 language="en"，提示用户清晰发音 |
| API 费用超预算 | 中 | 开发成本增加 | DeepSeek 降级方案，限制单次对话轮数 |
| 录音组件兼容性 | 低 | 功能不可用 | HTML5 MediaRecorder 备选方案 |
| 72 小时时间不足 | 高 | 功能不完备 | 严格按 PR 优先级开发，核心闭环优先 |

---

## 附录 A：项目文件清单

```
AI-Spoken-English-Trainer/
├── .env.example                # 环境变量模板（7 项配置）
├── .gitignore                  # Git 忽略规则
├── README.md                   # 项目完整文档（880 行）
├── requirements.txt            # Python 依赖
├── 需求分析.md                 # 需求文档
├── 需求分析说明书.md           # 需求文档（本文件）
├── backend/                    # FastAPI 后端
│   ├── main.py                 # FastAPI 主入口（7 个路由模块 + CORS）
│   ├── api/
│   │   ├── auth.py             # 认证接口（注册/登录/登出/GitHub OAuth）
│   │   ├── scenes.py           # 场景与难度配置接口
│   │   ├── sessions.py         # 会话管理 + 消息收发接口
│   │   ├── grammar.py          # 语法纠错接口
│   │   ├── pronunciation.py    # 发音评测接口
│   │   ├── tts.py              # TTS 语音合成接口
│   │   └── report.py           # 报告生成接口
│   ├── core/
│   │   └── auth.py             # JWT 认证核心模块
│   └── models/
│       └── schemas.py          # Pydantic 请求/响应模型（全接口）
├── config/
│   ├── __init__.py
│   ├── settings.py             # 全局配置（3 模型 + 3 场景 + 3 难度 + TTS 音色）
│   └── prompts.py              # 9 套场景×难度 Prompt + 模型风格注入
├── frontend/                   # Next.js 前端
│   ├── public/scenes/          # 场景背景图（3 场景 × 3 难度 = 9 张）
│   ├── src/
│   │   ├── app/
│   │   │   ├── (auth)/         # 认证路由组
│   │   │   │   ├── login/      # 登录页（邮箱 + GitHub OAuth）
│   │   │   │   └── register/   # 注册页
│   │   │   ├── (main)/         # 主应用路由组（认证守卫）
│   │   │   │   ├── home/       # 首页（场景引导）
│   │   │   │   ├── practice/   # 语音对话练习页
│   │   │   │   ├── grammar/    # 语法纠错独立页
│   │   │   │   ├── pronunciation/ # 发音评测独立页
│   │   │   │   ├── asr/        # 语音识别独立页
│   │   │   │   ├── tts/        # 语音合成独立页
│   │   │   │   ├── report/     # 课后报告页
│   │   │   │   └── history/    # 历史会话页 + 详情页
│   │   │   ├── globals.css     # 全局样式
│   │   │   └── layout.tsx      # 根布局
│   │   ├── components/
│   │   │   ├── navbar.tsx      # 顶部导航栏（8 项导航）
│   │   │   └── sidebar.tsx     # 侧边栏（场景/难度/模型/会话管理）
│   │   ├── lib/
│   │   │   └── api.ts          # Axios API 客户端
│   │   └── stores/
│   │       ├── auth-store.ts   # 用户认证状态（Zustand）
│   │       └── app-store.ts    # 应用全局状态（Zustand）
│   ├── package.json            # 前端依赖配置
│   ├── tsconfig.json           # TypeScript 配置
│   └── next.config.ts          # Next.js 配置
├── modules/                    # Python 核心模块
│   ├── llm.py                  # LLM 对话引擎（三模型 + 风格注入）
│   ├── grammar.py              # 语法纠错（8 类细分 + 双语解释）
│   ├── pronunciation.py        # 发音评测（Web Speech + LLM 文本对比）
│   ├── tts.py                  # TTS 合成（Edge → Azure → OpenAI 降级）
│   ├── evaluation.py           # 会话评估（六维评分 + 强弱项诊断）
│   └── report.py               # 报告生成（数据聚合 + AI 建议）
├── utils/
│   ├── __init__.py
│   └── db.py                   # SQLite 数据库 CRUD（6 表 + WAL 模式）
└── data/
    └── sessions.db             # SQLite 数据库文件
```

---

## 附录 B：竞赛评审对标

| 评审项 | 占比 | 实际完成情况 |
|--------|------|-------------|
| 作品完整度与创新性 | 40% | 5 项核心功能完整实现 + 四大创新维度 + 11 个页面 + 20 个 API |
| 开发过程与代码质量 | 40% | 18 次 PR 分阶段提交 + 模块化代码 + Commit 规范 |
| 演示与表达 | 20% | 完整 Demo 视频 + 百度网盘 + 880 行 README |

---

> 文档编写日期：2026-06-05（初版）→ 2026-06-07（更新至实际实现） | 版本：v2.0
