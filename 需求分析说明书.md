# AI英语口语陪练 — 需求分析说明书

> 版本：v1.0 | 日期：2026-06-05 | 作者：XEngineer 第三批参赛项目

---

## 目录

1. [项目概述](#1-项目概述)
2. [需求分析](#2-需求分析)
3. [技术架构](#3-技术架构)
4. [技术栈详解](#4-技术栈详解)
5. [功能模块详细设计](#5-功能模块详细设计)
6. [数据库设计](#6-数据库设计)
7. [API 集成方案](#7-api-集成方案)
8. [UI/UX 设计规范](#8-uiux-设计规范)
9. [开发计划与 PR 拆分](#9-开发计划与-pr-拆分)
10. [测试策略](#10-测试策略)
11. [部署方案](#11-部署方案)
12. [风险与应对](#12-风险与应对)

---

## 1. 项目概述

### 1.1 项目背景

本项目为 XEngineer 新工科计划第三批竞赛选题——**题目一：AI 英语口语陪练**。旨在开发一款基于 AI 的英语口语练习工具，帮助用户在真实场景中进行英语口语对话训练，并获得即时的发音评分、语法纠正和课后量化学习报告，实现口语能力的可量化提升。

### 1.2 项目目标

| 目标 | 描述 |
|------|------|
| 场景对话 | 在职场面试、餐厅点餐、商务会议 3 个真实场景中进行英文对话练习 |
| 实时交互 | 浏览器麦克风收音，端到端低延迟语音交互，自然流畅 |
| 智能评测 | 每轮对话后自动发音评测（0-100 分）+ 语法纠错 |
| 量化反馈 | 课后一键生成可视化学习报告，包含趋势图、错误分析、提升建议 |

### 1.3 开发约束

| 约束项 | 说明 |
|------|------|
| 开发周期 | 2026-06-05 00:00 ~ 2026-06-07 23:59（72 小时） |
| 提交方式 | 公开 GitHub 仓库 + 3 次分阶段 PR 提交 |
| 编程语言 | Python 3.10+ |
| 前端框架 | Streamlit（Python 全栈框架） |
| 交付物 | 可运行源码 + 完整 README + Demo 视频 |

---

## 2. 需求分析

### 2.1 功能性需求

#### F1. 场景选择与难度配置

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F1.1 | 3 个对话场景 | P0 必须 | 职场面试 / 餐厅点餐 / 商务会议 |
| F1.2 | 3 个难度档位 | P0 必须 | 初级 / 中级 / 高级 |
| F1.3 | 场景专属 AI 人设 | P0 必须 | 每个场景配置独立的 System Prompt |
| F1.4 | 难度自适应对话 | P0 必须 | AI 对话内容、语速、词汇随难度调整 |

**场景×难度矩阵（9 套配置）：**

| 场景 \ 难度 | 初级 Beginner | 中级 Intermediate | 高级 Advanced |
|-------------|---------------|-------------------|---------------|
| 职场面试 | 简单自我介绍、基础问答 | 行为面试、项目描述 | 技术深度讨论、薪资谈判 |
| 餐厅点餐 | 基础菜单词汇、简单句型 | 饮食需求、修改订单 | 投诉处理、定制需求 |
| 商务会议 | 简单议程讨论 | 进度汇报、意见表达 | 商务谈判、数据论证 |

#### F2. 实时语音对话

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F2.1 | 浏览器麦克风收音 | P0 必须 | 点击按钮开始/停止录音 |
| F2.2 | 流式语音识别 | P0 必须 | 实时将英文语音转为文本（ASR） |
| F2.3 | AI 英文回复 | P0 必须 | LLM 根据场景 Prompt 生成自然英文回复 |
| F2.4 | 语音合成播放 | P0 必须 | 将 AI 文本回复转为语音播放（TTS） |
| F2.5 | 低延迟交互 | P1 高优 | 端到端延迟控制在 3 秒以内 |
| F2.6 | 文本输入备用 | P2 中优 | 无麦克风时支持键盘文本输入 |

**核心对话流程：**

```
用户点击录音 → 浏览器采集音频 → Whisper API 转写英文文本
→ LLM 生成英文回复 → TTS 合成语音 → 浏览器播放语音
→ 同时显示文本对话记录
```

#### F3. 发音评测打分

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F3.1 | 总分 0-100 | P0 必须 | 每轮用户语音后自动评测 |
| F3.2 | 分项得分 | P0 必须 | 准确度 / 流利度 / 完整度 |
| F3.3 | 错误词标注 | P1 高优 | 高亮发音错误词 + 正确音标 |
| F3.4 | 颜色分级展示 | P2 中优 | 绿色≥80 / 黄色 60-80 / 红色<60 |

#### F4. 语法与表达纠错

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F4.1 | 语法错误检测 | P0 必须 | 自动检测时态、冠词、介词等错误 |
| F4.2 | 优化句展示 | P0 必须 | 原句（错误标注）+ 优化后标准句式 |
| F4.3 | 错误分类标注 | P1 高优 | 标注错误类型（grammar/vocabulary/tense...） |
| F4.4 | 不打断对话 | P0 必须 | 纠错以折叠卡片展示，不打断实时对话 |

#### F5. 课后量化总结报告

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| F5.1 | 整体平均分 | P0 必须 | 本次会话的整体发音平均分 |
| F5.2 | 发音趋势图 | P0 必须 | 每轮得分变化的折线图 |
| F5.3 | 错误分类饼图 | P0 必须 | 按错误类型分类的统计图表 |
| F5.4 | 高频错误 TOP5 | P0 必须 | 横向柱状图展示高频语法错误 |
| F5.5 | 薄弱知识点清单 | P1 高优 | 列出薄弱环节 + 例句 |
| F5.6 | AI 提升建议 | P1 高优 | 基于数据生成个性化口语提升建议 |
| F5.7 | 可视化图表 | P0 必须 | 使用 Plotly 柱状图/饼图/雷达图 |

### 2.2 非功能性需求

| ID | 需求 | 说明 |
|----|------|------|
| NF1 | 响应时间 | ASR 转写 < 2s，LLM 回复 < 3s，发音评测 < 2s |
| NF2 | 可用性 | 关键 API 失败时提供降级方案和用户提示 |
| NF3 | 兼容性 | 支持 Chrome / Edge 浏览器 |
| NF4 | 可扩展性 | 场景和难度 Prompt 配置化，易于新增 |
| NF5 | 代码质量 | 模块化分层，注释完善，符合 PEP 8 规范 |

---

## 3. 技术架构

### 3.1 架构总览图

```
┌──────────────────────────────────────────────────────────┐
│                    浏览器 (Browser)                       │
│  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 麦克风   │  │ 对话界面  │  │ 评分展示  │  │ 报告页面  │  │
│  │ 录音     │  │ Chat UI  │  │ Score UI │  │ Report   │  │
│  └────┬─────┘  └──────────┘  └──────────┘  └──────────┘  │
└───────┼──────────────────────────────────────────────────┘
        │ 音频流 (Web Audio API)
        ▼
┌──────────────────────────────────────────────────────────┐
│                  Streamlit 应用层 (Python)                 │
│                                                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ ASR 模块  │ │ LLM 模块 │ │ 发音评测  │ │ TTS 模块 │    │
│  │ Whisper  │ │GPT/Deep- │ │ Azure    │ │ OpenAI/  │    │
│  │ API      │ │Seek API  │ │ Pronun.  │ │ Azure    │    │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘    │
│       │            │            │            │           │
│  ┌────┴────────────┴────────────┴────────────┴────┐      │
│  │              数据持久化层 (SQLite)                │      │
│  │  sessions │ messages │ scores │ corrections     │      │
│  └────────────────────────────────────────────────┘      │
└──────────────────────────────────────────────────────────┘
        │                          │
        ▼                          ▼
┌──────────────┐    ┌──────────────────────────┐
│ OpenAI API   │    │ Azure Speech Services    │
│ · GPT-4o     │    │ · Pronunciation Assess.  │
│ · Whisper-1  │    │ · Text-to-Speech         │
│ · TTS-1      │    └──────────────────────────┘
└──────────────┘
```

### 3.2 数据流图

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ 用户点击  │───→│ 录音采集  │───→│ ASR转写  │───→│ LLM对话  │───→│ TTS播放  │
│ 录音按钮  │    │ (浏览器)  │    │ (Whisper)│    │ (GPT-4o) │    │ (OpenAI) │
└──────────┘    └──────────┘    └────┬─────┘    └────┬─────┘    └──────────┘
                                     │               │
                          ┌──────────┘               └──────────┐
                          ▼                                     ▼
                    ┌──────────┐                        ┌──────────┐
                    │ 发音评测  │                        │ 语法纠错  │
                    │ (Azure)  │                        │ (LLM)    │
                    └────┬─────┘                        └────┬─────┘
                         │                                   │
                         └───────────────┬───────────────────┘
                                         ▼
                                   ┌──────────┐
                                   │ 数据存储  │
                                   │ (SQLite) │
                                   └────┬─────┘
                                        │
                                        ▼
                                   ┌──────────┐
                                   │ 报告生成  │
                                   │ (Plotly) │
                                   └──────────┘
```

---

## 4. 技术栈详解

### 4.1 前端框架：Streamlit

| 属性 | 说明 |
|------|------|
| 版本 | >= 1.31.0 |
| 选型理由 | Python 全栈框架，无需前端 HTML/JS 开发；内置 session_state 状态管理；丰富的 UI 组件 |
| 使用方式 | `streamlit run app.py` 一键启动，自动打开浏览器 |

**Streamlit 核心功能在本项目中的使用：**

| 功能 | Streamlit API | 用途 |
|------|---------------|------|
| 页面配置 | `st.set_page_config()` | 设置页面标题、图标、布局 |
| 侧边栏 | `st.sidebar` | 场景选择、难度选择、模型切换 |
| 下拉选择 | `st.selectbox()` | 场景选择下拉框 |
| 单选按钮 | `st.radio()` | 难度档位、模型切换 |
| 按钮 | `st.button()` | 新建会话、结束会话、生成报告 |
| 输入框 | `st.chat_input()` | 文本输入备用方案 |
| 状态管理 | `st.session_state` | 全局状态（当前场景/难度/模型/消息列表） |
| Chat 组件 | `st.chat_message()` | 对话气泡展示 |
| 图表渲染 | `st.plotly_chart()` | 报告页图表展示 |
| 自定义样式 | `st.markdown()` + CSS | 自定义 UI 样式 |
| 错误提示 | `st.error()` / `st.info()` | 异常状态提示 |

**Streamlit 限制与应对：**

| 限制 | 应对方案 |
|------|---------|
| 不支持 WebSocket | 使用 HTTP 请求轮询或 streamlit-webrtc 组件 |
| 每次交互重跑脚本 | 用 `st.session_state` 保持状态 |
| 无原生录音组件 | 通过 `st.markdown()` 嵌入 HTML5/JS 录音组件 |

### 4.2 语音识别：OpenAI Whisper API

| 属性 | 说明 |
|------|------|
| 模型 | `whisper-1` |
| 选型理由 | 英文识别准确率极高（词错率 ~5%）；API 调用简单；支持多种英语口音 |
| 输入 | 音频文件（wav/mp3/webm），最大 25MB |
| 输出 | 转写后的英文文本 |
| 费用 | $0.006 / 分钟 |

**使用流程：**

```python
import openai

def transcribe_audio(audio_file_path: str) -> str:
    """使用 Whisper API 将音频转为文本"""
    with open(audio_file_path, "rb") as audio_file:
        transcript = openai.Audio.transcribe(
            model="whisper-1",
            file=audio_file,
            language="en",          # 指定英语提高准确率
            response_format="text"
        )
    return transcript
```

**优化策略：**

| 策略 | 说明 |
|------|------|
| 指定语言 | `language="en"` 提高英文识别准确率 |
| 音频预处理 | 16kHz 采样率、单声道、WAV 格式 |
| 音频分片 | 长音频按句子分割，减少单次请求大小 |
| 错误重试 | 网络异常时自动重试 3 次 |

### 4.3 大语言模型：OpenAI GPT-4o / DeepSeek

#### 4.3.1 OpenAI GPT-4o（主力模型）

| 属性 | 说明 |
|------|------|
| 模型 | `gpt-4o` |
| 选型理由 | 对话最自然流畅；英文理解/生成能力顶级；支持流式输出 |
| 费用 | 输入 $2.50/1M tokens，输出 $10.00/1M tokens |

**使用流程：**

```python
from openai import OpenAI

client = OpenAI(api_key="sk-xxx")

def generate_reply(messages: list, scene_prompt: str) -> str:
    """使用 GPT-4o 生成英文回复"""
    system_msg = {"role": "system", "content": scene_prompt}
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[system_msg] + messages,
        temperature=0.7,       # 保持一定创意性
        max_tokens=150,        # 限制回复长度
        stream=False           # PR2 阶段可开启 streaming
    )
    return response.choices[0].message.content
```

**System Prompt 设计要点：**

- 明确 AI 角色和场景
- 设定对话风格、语速、词汇难度
- 限制回复长度（2-4 句）
- 要求只使用英文
- 引导对话自然进行

#### 4.3.2 DeepSeek（备用模型）

| 属性 | 说明 |
|------|------|
| 模型 | `deepseek-chat` |
| 选型理由 | 国产大模型，成本极低（¥1/百万 tokens）；英文能力良好 |
| Base URL | `https://api.deepseek.com` |
| 费用 | 输入 ¥1/1M tokens，输出 ¥2/1M tokens |

**使用流程：**

```python
from openai import OpenAI

# DeepSeek 兼容 OpenAI SDK
client = OpenAI(
    api_key="sk-xxx",
    base_url="https://api.deepseek.com"
)

# 调用方式与 OpenAI 完全相同
response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[...]
)
```

**双模型切换方案：**

```python
# config/settings.py
DEFAULT_MODEL = "openai"  # 或 "deepseek"

# modules/llm.py
def get_llm_client():
    if st.session_state.current_model == "openai":
        return OpenAI(api_key=OPENAI_API_KEY)
    else:
        return OpenAI(
            api_key=DEEPSEEK_API_KEY,
            base_url=DEEPSEEK_BASE_URL
        )

def get_model_name():
    if st.session_state.current_model == "openai":
        return "gpt-4o"
    else:
        return "deepseek-chat"
```

### 4.4 发音评测：Azure Pronunciation Assessment

| 属性 | 说明 |
|------|------|
| 服务 | Azure Speech Services - Pronunciation Assessment |
| SDK | `azure-cognitiveservices-speech` >= 1.35.0 |
| 选型理由 | 业界最成熟的发音评测 API；输出 0-100 分 + 音素级错误；支持流利度/完整度/准确度分项 |
| 免费额度 | 每月 5 小时音频 |
| 付费 | 超出后按使用量计费 |

**使用流程：**

```python
import azure.cognitiveservices.speech as speechsdk

def assess_pronunciation(audio_file: str, reference_text: str) -> dict:
    """使用 Azure 发音评测 API"""
    speech_config = speechsdk.SpeechConfig(
        subscription=AZURE_SPEECH_KEY,
        region=AZURE_SPEECH_REGION
    )

    # 配置发音评测
    pronunciation_config = speechsdk.PronunciationAssessmentConfig(
        reference_text=reference_text,    # 参考文本（用户的转写结果）
        grading_system="HundredMark",     # 百分制评分
        granularity="Phoneme",            # 音素级颗粒度
        enable_miscue=True                # 启用误读检测
    )

    audio_config = speechsdk.AudioConfig(filename=audio_file)
    recognizer = speechsdk.SpeechRecognizer(
        speech_config=speech_config,
        audio_config=audio_config
    )

    pronunciation_config.apply_to(recognizer)

    result = recognizer.recognize_once()

    # 解析结果
    pronunciation_result = speechsdk.PronunciationAssessmentResult(result)

    return {
        "overall_score": pronunciation_result.pronunciation_score,
        "accuracy_score": pronunciation_result.accuracy_score,
        "fluency_score": pronunciation_result.fluency_score,
        "completeness_score": pronunciation_result.completeness_score,
        "errors": _parse_word_errors(pronunciation_result)
    }
```

**返回数据结构：**

```json
{
    "overall_score": 78.5,
    "accuracy_score": 82.0,
    "fluency_score": 75.0,
    "completeness_score": 80.0,
    "errors": [
        {
            "word": "pronunciation",
            "correct_phoneme": "/prəˌnʌn.siˈeɪ.ʃən/",
            "error_type": "Mispronunciation"
        }
    ]
}
```

### 4.5 语音合成：OpenAI TTS / Azure TTS

#### OpenAI TTS（主力）

| 属性 | 说明 |
|------|------|
| 模型 | `tts-1` |
| 音色 | `alloy`（自然中性英音） |
| 格式 | mp3 / opus / aac / flac |
| 费用 | $15.00 / 1M 字符 |

**使用流程：**

```python
from openai import OpenAI

def text_to_speech(text: str, output_path: str) -> str:
    """使用 OpenAI TTS 将文本转为语音"""
    client = OpenAI(api_key=OPENAI_API_KEY)
    response = client.audio.speech.create(
        model="tts-1",
        voice="alloy",          # 自然英音
        input=text
    )
    response.stream_to_file(output_path)
    return output_path
```

#### Azure TTS（备用）

与 Azure Speech 共用 SDK，无需额外配置。

### 4.6 数据可视化：Plotly

| 属性 | 说明 |
|------|------|
| 版本 | >= 5.18.0 |
| 选型理由 | 交互式图表；支持柱状图/饼图/折线图/雷达图；与 Streamlit 原生集成 |
| 使用方式 | `st.plotly_chart(fig)` 直接渲染 |

**图表类型规划：**

| 图表 | 类型 | 数据来源 |
|------|------|---------|
| 发音趋势图 | 折线图 `go.Scatter` | `pronunciation_scores` 表 |
| 分项雷达图 | 极坐标图 `go.Scatterpolar` | 发音分项平均值 |
| 错误分类饼图 | 饼图 `go.Pie` | `grammar_corrections` 表 |
| 高频错误 TOP5 | 横向柱状图 `go.Bar` | `grammar_corrections` 表 |

### 4.7 数据存储：SQLite

| 属性 | 说明 |
|------|------|
| 选型理由 | Python 内置支持，零配置；轻量级，适合本地运行；单文件数据库 |
| 数据库文件 | `data/sessions.db` |
| ORM | 直接使用 `sqlite3` 标准库，row_factory 返回 dict |

---

## 5. 功能模块详细设计

### 5.1 模块划分

```
modules/
├── asr.py           # 语音识别模块
│   ├── record_audio()         # 浏览器端录音
│   ├── save_audio()           # 保存音频文件
│   └── transcribe()           # Whisper 转写
│
├── llm.py           # 大模型对话模块
│   ├── get_client()           # 获取 LLM 客户端（自动切换）
│   ├── get_model_name()       # 获取当前模型名称
│   └── generate_reply()       # 生成 AI 英文回复
│
├── pronunciation.py # 发音评测模块
│   ├── assess()               # Azure 发音评测
│   ├── parse_result()         # 解析评测结果
│   └── format_errors()        # 格式化错误信息
│
├── grammar.py       # 语法纠错模块
│   ├── detect_errors()        # LLM 语法检测
│   ├── parse_correction()     # 解析纠错结果
│   └── format_correction()    # 格式化展示
│
├── tts.py           # 语音合成模块
│   ├── synthesize()           # 文本转语音
│   └── get_tts_provider()     # 获取 TTS 服务商
│
└── report.py        # 报告生成模块
    ├── aggregate_data()       # 聚合数据
    ├── generate_charts()      # 生成 Plotly 图表
    └── generate_suggestions() # AI 生成提升建议
```

### 5.2 音频处理模块

```python
# utils/audio.py

import wave
import pyaudio
import numpy as np

class AudioConfig:
    """音频采集配置"""
    FORMAT = pyaudio.paInt16
    CHANNELS = 1        # 单声道
    RATE = 16000        # 16kHz 采样率
    CHUNK = 1024        # 缓冲区大小
    RECORD_SECONDS = 10 # 最大录音时长（秒）

def record_audio(output_path: str, duration: int = None) -> str:
    """录制音频到文件"""
    ...

def convert_audio_format(input_path: str, output_format: str = "wav") -> str:
    """音频格式转换"""
    ...

def get_audio_duration(file_path: str) -> float:
    """获取音频时长"""
    ...
```

---

## 6. 数据库设计

### 6.1 ER 图（逻辑设计）

```
┌──────────────┐        ┌──────────────┐
│   sessions   │ 1───N │   messages   │
│              │       │              │
│ id (PK)      │       │ id (PK)      │
│ scene_key    │       │ session_id   │──→ FK → sessions.id
│ scene_name   │       │ role         │
│ difficulty   │       │ content      │
│ model        │       │ created_at   │
│ created_at   │       └──────┬───────┘
│ ended_at     │              │ 1───0..1
│ total_rounds │    ┌─────────┴─────────┐
│ avg_score    │    │                   │
│ status       │    ▼                   ▼
└──────────────┘  ┌──────────────────┐ ┌──────────────────────┐
                  │pronunciation     │ │ grammar_corrections  │
                  │    _scores       │ │                      │
                  │                  │ │ id (PK)              │
                  │ id (PK)          │ │ session_id (FK)      │
                  │ session_id (FK)  │ │ message_id (FK)      │
                  │ message_id (FK)  │ │ original_text        │
                  │ overall_score    │ │ corrected_text       │
                  │ accuracy_score   │ │ error_type           │
                  │ fluency_score    │ │ explanation          │
                  │ completeness     │ │ created_at           │
                  │ error_details    │ └──────────────────────┘
                  │ created_at       │
                  └──────────────────┘
```

### 6.2 表结构详细设计

#### 表 1: sessions（会话表）

```sql
CREATE TABLE sessions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    scene_key       TEXT NOT NULL,          -- 场景 key: job_interview/restaurant/business_meeting
    scene_name      TEXT NOT NULL,          -- 场景中文名
    difficulty      TEXT NOT NULL,          -- 难度: beginner/intermediate/advanced
    model           TEXT NOT NULL,          -- 模型: openai/deepseek
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    ended_at        TIMESTAMP,             -- 结束时间（NULL=进行中）
    total_rounds    INTEGER DEFAULT 0,     -- 总对话轮数
    avg_pronunciation_score REAL DEFAULT 0.0, -- 平均发音分
    status          TEXT DEFAULT 'active'  -- active/ended
);
```

#### 表 2: messages（消息表）

```sql
CREATE TABLE messages (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id      INTEGER NOT NULL,      -- FK → sessions.id
    role            TEXT NOT NULL,         -- user / ai
    content         TEXT NOT NULL,         -- 消息内容
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE
);
```

#### 表 3: pronunciation_scores（发音评分表）

```sql
CREATE TABLE pronunciation_scores (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id      INTEGER NOT NULL,      -- FK → sessions.id
    message_id      INTEGER NOT NULL,      -- FK → messages.id
    overall_score   REAL NOT NULL,         -- 总分 0-100
    accuracy_score  REAL,                  -- 准确度
    fluency_score   REAL,                  -- 流利度
    completeness_score REAL,               -- 完整度
    error_details   TEXT,                  -- JSON: [{word, phoneme, error_type}]
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE,
    FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE
);
```

#### 表 4: grammar_corrections（语法纠错表）

```sql
CREATE TABLE grammar_corrections (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id      INTEGER NOT NULL,      -- FK → sessions.id
    message_id      INTEGER NOT NULL,      -- FK → messages.id
    original_text   TEXT NOT NULL,         -- 用户原句
    corrected_text  TEXT NOT NULL,         -- 纠正后句子
    error_type      TEXT,                  -- 错误类型
    explanation     TEXT,                  -- 错误解释
    created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES sessions(id) ON DELETE CASCADE,
    FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE
);
```

---

## 7. API 集成方案

### 7.1 API 配置清单

| API | 环境变量 | 用途 | 申请地址 |
|-----|---------|------|---------|
| OpenAI API Key | `OPENAI_API_KEY` | GPT-4o + Whisper + TTS | https://platform.openai.com |
| DeepSeek API Key | `DEEPSEEK_API_KEY` | 备用 LLM | https://platform.deepseek.com |
| Azure Speech Key | `AZURE_SPEECH_KEY` | 发音评测 | https://portal.azure.com |
| Azure Region | `AZURE_SPEECH_REGION` | 服务区域 | eastasia（东亚） |

### 7.2 环境变量管理

```ini
# .env 文件（不提交到 Git）
OPENAI_API_KEY=sk-your-openai-api-key
DEEPSEEK_API_KEY=sk-your-deepseek-api-key
AZURE_SPEECH_KEY=your-azure-speech-key
AZURE_SPEECH_REGION=eastasia
DEFAULT_MODEL=openai
```

### 7.3 错误处理与重试策略

| 场景 | 策略 |
|------|------|
| API 超时 | 设置 30s 超时，重试 3 次 |
| API 限额 | 提示用户 "API 配额已用尽，请稍后再试" |
| 网络异常 | 显示降级提示，保留文本输入备用通道 |
| 音频不可用 | 显示 "未检测到麦克风设备"，切换到文本输入模式 |
| 发音评测失败 | 显示 "评分暂时不可用"，不影响对话继续 |

---

## 8. UI/UX 设计规范

### 8.1 设计风格

| 属性 | 值 |
|------|-----|
| 设计风格 | 现代简约教育类应用 |
| 主色调 | 蓝色系（#667eea → #764ba2） |
| 背景 | 浅灰渐变（#f5f7fa → #c3cfe2） |
| 侧边栏 | 深色（#1a1a2e → #0f3460） |
| 字体 | Inter（Sans-serif） |
| 圆角 | 8px-18px |
| 阴影 | 柔和投影（透明度 6%-8%） |

### 8.2 页面布局

```
┌──────────┬─────────────────────────────────────────┐
│          │  ┌─────────────────────────────────┐    │
│ 侧边栏   │  │   English Conversation Practice  │    │
│ 300px    │  ├─────────────────────────────────┤    │
│          │  │                                  │    │
│ 🎤 场景  │  │  💬 AI: Welcome to the...        │    │
│ 选择     │  │                                  │    │
│          │  │  💬 You: I graduated from...     │    │
│ 📊 难度  │  │                                  │    │
│ 选择     │  │  📊 Pronunciation: 82分          │    │
│          │  │  📝 Grammar: 1 correction        │    │
│ 🤖 模型  │  │                                  │    │
│ 选择     │  │  💬 AI: That's great! Can...     │    │
│          │  │                                  │    │
│ ──────── │  ├─────────────────────────────────┤    │
│ 会话管理  │  │       🎤 [点击开始说话]         │    │
│ [新建]  │  │     Type your message...          │    │
│ [结束]  │  └─────────────────────────────────┘    │
│ ──────── │                                         │
│ 📈 报告  │                                         │
│ [生成]  │                                         │
└──────────┴─────────────────────────────────────────┘
```

### 8.3 交互规范

| 交互 | 规范 |
|------|------|
| 场景切换 | 弹出确认提示，清空对话历史 |
| 新建会话 | 自动结束上一会话，创建新会话 |
| 录音按钮 | 红色脉冲动画，录音中高亮 |
| 发音评分 | 即时展示，颜色分级 |
| 语法纠错 | 折叠卡片，点击展开 |
| 报告生成 | 独立页面渲染，支持滚动 |

### 8.4 颜色语义

| 颜色 | 含义 | 使用场景 |
|------|------|---------|
| 绿色 #00b894 | 优秀 | 评分 ≥ 80 |
| 黄色 #f39c12 | 良好 | 评分 60-80 |
| 红色 #d63031 | 需改进 | 评分 < 60 |
| 蓝色 #2196f3 | 信息 | 状态提示 |
| 橙色 #ff9800 | 警告 | 纠错卡片 |

---

## 9. 开发计划与 PR 拆分

### 9.1 PR 拆分总览

| PR | 日期 | 分支名 | 核心内容 |
|----|------|--------|---------|
| PR1 | 6.5 | `pr1-project-setup` | 项目初始化、框架搭建、配置 |
| PR2 | 6.6 | `pr2-voice-interaction` | 语音交互闭环、语法纠错 |
| PR3 | 6.7 | `pr3-pronunciation-report` | 发音评测、报告生成、UI优化 |

### 9.2 PR1 详细任务清单（6.5 — 已完成）

- [x] 项目目录结构搭建
- [x] `requirements.txt` 依赖配置
- [x] `config/settings.py` 全局配置加载
- [x] `config/prompts.py` 9 套场景×难度 Prompt
- [x] `utils/db.py` SQLite 数据库 CRUD 封装
- [x] `ui/sidebar.py` 侧边栏组件
- [x] `ui/chat.py` 对话界面组件
- [x] `assets/style.css` 自定义样式
- [x] `app.py` 主入口文件
- [x] `.env.example` 环境变量模板
- [x] `.gitignore`
- [x] `README.md` 完整文档

### 9.3 PR2 详细任务清单（6.6 — 待开始）

**模块开发：**

- [ ] `utils/audio.py` — 音频录制/格式转换工具
- [ ] `modules/asr.py` — Whisper API 语音转写
- [ ] `modules/llm.py` — OpenAI/DeepSeek 双模型对话
- [ ] `modules/tts.py` — TTS 文本转语音合成
- [ ] `modules/grammar.py` — LLM 语法纠错

**UI 开发：**

- [ ] `ui/recorder.py` — 录音按钮组件
- [ ] 对话流程集成（录音→转写→LLM→TTS→纠错展示）
- [ ] 文本输入备用通道

**数据库：**

- [ ] 消息记录实时写入
- [ ] 纠错记录存储

**目标：** 完成 "说话→转写→AI回复→语音播放→纠错" 完整闭环

### 9.4 PR3 详细任务清单（6.7 — 待开始）

**发音评测：**

- [ ] `modules/pronunciation.py` — Azure 发音评测集成
- [ ] 每轮对话后自动评测
- [ ] 评分卡片 UI 展示

**报告生成：**

- [ ] `modules/report.py` — 数据聚合 + 图表生成
- [ ] `ui/report.py` — 报告页面渲染
- [ ] AI 提升建议生成

**UI 优化：**

- [ ] 加载动画
- [ ] 状态提示优化
- [ ] 响应式适配
- [ ] 异常容错处理

**最终交付：**

- [ ] 端到端测试
- [ ] README 最终版
- [ ] Demo 视频录制

---

## 10. 测试策略

### 10.1 测试范围

| 测试类型 | 内容 |
|---------|------|
| 单元测试 | 各模块独立功能验证 |
| 集成测试 | API 调用链路验证 |
| 端到端测试 | 完整用户操作流程 |
| 异常测试 | API 失败、网络异常、无效输入 |

### 10.2 测试用例

| ID | 场景 | 操作 | 预期结果 |
|----|------|------|---------|
| T1 | 场景切换 | 选择不同场景 | 场景更新，历史清空 |
| T2 | 新建会话 | 点击「新建会话」 | 数据库创建 session 记录 |
| T3 | 语音转写 | 录入英文语音 | 正确转写为英文文本 |
| T4 | AI 对话 | 发送英文文本 | AI 返回场景相关的英文回复 |
| T5 | 发音评测 | 每次用户语音后 | 显示 0-100 分 + 分项得分 |
| T6 | 语法纠错 | 输入含错英文 | 显示原句/纠正句/错误解释 |
| T7 | 报告生成 | 点击「生成报告」 | 展示图表 + 建议清单 |
| T8 | 模型切换 | 切换 OpenAI/DeepSeek | 对话模型切换生效 |
| T9 | 网络异常 | 断开网络后操作 | 显示友好错误提示 |
| T10 | 无麦克风 | 无麦克风设备 | 自动切换到文本输入模式 |

---

## 11. 部署方案

### 11.1 本地部署

```bash
# 克隆仓库
git clone https://github.com/linyuchao123/AI-Spoken-English-Trainer.git
cd AI-Spoken-English-Trainer

# 创建虚拟环境
python -m venv venv
venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
cp .env.example .env
# 编辑 .env 填入 API 密钥

# 启动
streamlit run app.py
```

### 11.2 Streamlit Cloud 部署（可选）

1. 将代码推送到 GitHub 公开仓库
2. 访问 https://share.streamlit.io
3. 连接 GitHub 仓库
4. 配置 Secrets（API 密钥）
5. 一键部署

---

## 12. 风险与应对

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|---------|
| Azure 发音评测延迟高 | 中 | 用户体验差 | 异步调用 + 加载动画 |
| Whisper 英文口音不准 | 低 | 对话质量下降 | 指定 language="en"，提示用户清晰发音 |
| API 费用超预算 | 中 | 开发成本增加 | DeepSeek 降级方案，限制单次对话轮数 |
| 录音组件兼容性 | 低 | 功能不可用 | HTML5 MediaRecorder 备选方案 |
| 72 小时时间不足 | 高 | 功能不完备 | 严格按 PR 优先级开发，核心闭环优先 |

---

## 附录 A：项目文件清单

```
AI-Spoken-English-Trainer/
├── .env.example          # 环境变量模板
├── .gitignore            # Git 忽略规则
├── README.md             # 项目文档
├── requirements.txt      # Python 依赖
├── 需求分析.md           # 需求文档（本文件）
├── app.py                # Streamlit 主入口
├── assets/
│   └── style.css         # 自定义 CSS
├── config/
│   ├── __init__.py
│   ├── settings.py       # 全局配置
│   └── prompts.py        # 场景 Prompt 模板
├── data/
│   └── .gitkeep          # 数据库目录占位
├── modules/
│   ├── __init__.py
│   ├── asr.py            # [PR2] 语音识别
│   ├── llm.py            # [PR2] LLM 对话
│   ├── tts.py            # [PR2] 语音合成
│   ├── grammar.py        # [PR2] 语法纠错
│   ├── pronunciation.py  # [PR3] 发音评测
│   └── report.py         # [PR3] 报告生成
├── ui/
│   ├── __init__.py
│   ├── sidebar.py        # 侧边栏组件
│   ├── chat.py           # 对话界面
│   ├── recorder.py       # [PR2] 录音按钮
│   └── report.py         # [PR3] 报告页面
└── utils/
    ├── __init__.py
    ├── audio.py          # [PR2] 音频工具
    └── db.py             # 数据库操作
```

---

## 附录 B：竞赛评审对标

| 评审项 | 占比 | 应对措施 |
|--------|------|---------|
| 作品完整度与创新性 | 40% | 5 项核心功能完整实现 + 难度档位创新 + 可视化图表 |
| 开发过程与代码质量 | 40% | 3 次 PR 分阶段提交 + 模块化代码 + 注释完善 |
| 演示与表达 | 20% | 完整 Demo 视频 + 人声讲解 + 全流程演示 |

---

> 文档编写日期：2026-06-05 | 版本：v1.0
